name: Test Workflow Recognition

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Dummy Step
        run: echo "GitHub Actions Workflow wird erkannt!"